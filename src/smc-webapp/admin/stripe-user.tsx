/*
Functionality and UI to ensure a user with given email (or account_id) is sync'd with stripe.
*/

import { React, Component } from "../frame-editors/generic/react";

export class StripeUser extends Component {
  render() {
    return (
      <div>
        Stripe User
      </div>
    );
  }
}
