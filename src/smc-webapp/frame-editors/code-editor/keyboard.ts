/*
Keyboard shortcuts that are general to the whole frame tree.

*/

/*
NOTHING implemented yet!  since we really don't do anything at this level.
Could be used for keyboard shortcuts for switching between frames, etc.

export function create_key_handler(actions) {
    return function(evt) {
      return;

        const read_only = !!actions.store.get('read_only');
        const mod = evt.ctrlKey || evt.metaKey || evt.altKey || evt.shiftKey;

        if (read_only) {
            return;
        }

    };
}
*/
