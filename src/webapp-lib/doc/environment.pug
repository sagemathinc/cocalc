
extends ../_base.pug

block vars
  - subtitle = "Compute Environment"

block header
  meta(name="description" content=NAME + " Compute Environment")

block content
  a.anchor#a-top
  div.container
    div.row#top
      h1 Compute Environment

      ul
        li
          a(href="environment-python.html") Python


  //- CSS comes at the bottom: overwrites styles defined in the header
  style
    :sass
      @import "smc-webapp/_colors.sass"
      div#top
        margin           : 5rem 0
