// CoCalcPlus is a thin product wrapper over the Lite core.
// For now we simply re-export Lite so downstream consumers can
// depend on the product package without duplicating implementation.
export * from "@cocalc/lite";
