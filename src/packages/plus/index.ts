// CoCalcPlus is a thin product wrapper over the Lite core.
// Re-export the Lite main entry so downstream consumers can depend on
// the product package without duplicating implementation.
export * from "@cocalc/lite/main";
