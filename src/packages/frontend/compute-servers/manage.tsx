export default function ManageComputeServers({ project_id }) {
  return (
    <div>
      TODO: Description and list of servers associated to {project_id}, along
      with their status.
    </div>
  );
}
