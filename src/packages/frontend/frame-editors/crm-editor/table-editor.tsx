// not sure if will use this...
import Accounts from "./accounts";
//import People from "./people";

export default function TableEditor({}) {
  return <Accounts />;
  //return <pre>{JSON.stringify(desc, undefined, 2)}</pre>;
}
