// Entry point placeholder for the multi-project host service.
// This package will compose Lite + runner + file-server + project-proxy
// for a multi-tenant, podman/btrfs-backed host. For now, it is a formal
// dependency stub so we can layer functionality incrementally.
