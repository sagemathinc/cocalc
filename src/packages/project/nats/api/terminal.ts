import {
  createTerminal as create,
  restartTerminal as restart,
  terminalCommand as command,
  writeToTerminal as write,
} from "@cocalc/project/nats/terminal";

export { create, restart, command, write };
