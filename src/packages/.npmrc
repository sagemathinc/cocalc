# Unfortunately we currently have to do this hoisting so that webpack works.
# At least, the way it is configured for the frontend in static/webpack.config.js.
# This copies all the modules to packages/node_modules and we then point
# webpack at that in static/webpack.config.js.

hoist=true
shamefully-hoist=true
