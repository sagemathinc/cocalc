{
  "name": "@cocalc/cdn",
  "version": "1.15.1",
  "description": "Files that CoCalc uses that would be natural in various contexts to get from a CDN",
  "main": "dist/index.js",
  "keywords": ["cdn", "cocalc"],
  "devDependencies": {
    "bootstrap": "=3.4.1",
    "codemirror": "^5.65.3",
    "jest": "^29.3.1",
    "katex": "^0.15.0",
    "mathjax": "^2.7.9",
    "pdfjs-dist": "^2.13.216",
    "typescript": "^4.7.4"
  },
  "scripts": {
    "clean": "rm -rf build node_modules dist",
    "build": "rm -rf build dist && mkdir build && cp -rv *.json *.py *.js pix build/ && cd build && npm ci && mkdir dist && ./setup.py && mv dist .. && cd .. && rm -rf build ",
    "postinstall": "./postinstall.js",
    "test": "npx jest",
    "prepublishOnly": "npm test"
  },
  "homepage": "https://github.com/sagemathinc/cocalc/tree/master/src/packages/cdn",
  "repository": {
    "type": "git",
    "url": "https://github.com/sagemathinc/cocalc"
  },
  "author": "SageMath, Inc.",
  "license": "SEE LICENSE.md"
}
