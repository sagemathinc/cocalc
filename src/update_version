#!/usr/bin/env bash

source smc-env

cd $SALVUS_ROOT

set -e

# Create version file, based on the current time
echo "exports.version=`date +"%s"`; /* autogenerated by the update_version script */" > smc-util/smc-version.js

