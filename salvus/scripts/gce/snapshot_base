
set -v

export NAME=compute-base-disk-`date +"%Y-%m-%d-%H%M"`

gcutil --service_version="v1" --project="sagemathcloud" addsnapshot --source_disk "compute-base-disk" --wait_until_complete $NAME

echo $NAME>BASE

