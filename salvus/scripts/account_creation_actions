if [ "$1"x = "x" ]; then
    echo "Usage: account_creation_actions [email_address]"
    exit 1
else
    cd /home/salvus/salvus/salvus&& . salvus-env&& ./hub start --account_creation_actions  --database_nodes 10.1.10.2,10.1.3.2,10.1.4.2 --keyspace salvus
fi
